# 작업내역 - 2026.02.10 (교육 도구 3종 업데이트)

## 개요
마케팅 학교 키오스크 앱의 1~3교시 교육 도구 대폭 업데이트
- 대상: 이주민/외국인 마케팅 입문자 (마케팅 경험 없는 사람)
- 핵심: 쉬운 한국어, 인간 성향 기반 적성검사, 시장분석 레포트 추가

---

## 요구사항 요약

### 도구 이름 변경
| 교시 | 기존 이름 | 변경 이름 |
|------|-----------|-----------|
| 1교시 | 마케팅 적성검사 | **적성 검사기** |
| 2교시 | Market Compass 스캐너 | **시장 탐색기** |
| 3교시 | Market Compass 엣지메이커 | **브랜드 만들기** |

### 페르소나 매핑 (확정)
| ID | 이름 (~하는 사람) | 이모지 | 마케팅 영역 |
|----|-------------------|--------|-------------|
| CEO | 큰그림 그리는 사람 | 🦁 | 브랜드 마케팅 |
| PM | 이야기 만드는 사람 | 💡 | 콘텐츠 마케팅 |
| CPO | 숫자로 말하는 사람 | 📊 | 퍼포먼스 마케팅 |
| CMO | 사람 모으는 사람 | 📣 | CRM 마케팅 |
| CSL | 물건 파는 사람 | 🤝 | 라이브커머스 |

### 마케팅 영역 (5개만)
퍼포먼스마케팅, CRM마케팅, 라이브커머스, 브랜드마케팅, 콘텐츠마케팅

---

## 완료된 작업 (✅)

### 1. 타입 수정 - `src/types/school.ts`
- `AptitudeResult`에 `questionSetId?: string` 추가 (질문 세트 ID 기록)
- `MarketScannerResult.input`에 `itemType?: string` 추가
- `MarketScannerResult.output`에 `analysisReport?: string` 추가
- `EdgeMakerResult.input`에 `competitors?: CompetitorInfo[]` 추가
- `EdgeMakerResult.output`에 `brandingReport?: string` 추가
- 모든 신규 필드는 `?` (optional) → 기존 localStorage 데이터 호환

### 2. 적성검사 질문 3세트 구조 - `src/data/aptitudeQuestions.ts`
- 기존 `QUESTIONS` 단일 배열 → `QUESTION_SETS` (3세트 × 15문항) 구조로 변경
- 새 타입: `QuestionSetId = 'set1' | 'set2' | 'set3'`, `AptitudeQuestionSet`
- 새 헬퍼: `getQuestionSet(previousSetId?)` - 이전과 다른 세트 자동 선택
- `calculateResult(answers, questions)` - questions 파라미터 추가
- 하위호환 유지: `export const QUESTIONS = QUESTION_SETS[0].questions`
- 각 세트 15문항, CEO/PM/CPO/CMO/CSL 균등 가중치 분배
- i18n 키: `school.aptitude.questions.set1.Q1` ~ `set3.Q15`

### 3. AI 프롬프트 + mock 데이터 - `src/services/gemini/marketCompassService.ts`
- `generateMarketAnalysis()`: 4번째 파라미터 `itemType` 추가 (default: 'other')
  - 프롬프트에 아이템 형태 포함, `analysisReport` 필드 요청 (20줄, 쉬운 한국어)
- `generateBrandingStrategy()`: 3번째 파라미터 `competitors: CompetitorInfo[]` 추가
  - 프롬프트에 경쟁사 정보 포함, `brandingReport` 필드 요청 (20줄, 쉬운 한국어)
- mock 데이터 3종 (food, beauty, default) 모두 20줄 `analysisReport` 포함
- mock 브랜딩 데이터도 20줄 `brandingReport` 포함

### 4. 적성검사 도구 페이지 - `src/pages/marketing/school/tools/AptitudeTestTool.tsx`
- `getQuestionSet(previousResult?.questionSetId)`으로 세트 선택
- `activeQuestions` / `activeSetId` state 추가
- 결과 저장 시 `questionSetId` 포함
- `calculateResult(answers, activeQuestions)` 호출
- 설명 텍스트 `whitespace-pre-line` CSS 적용 (10줄 표시)
- `activeQuestions.length > 0` 가드 추가

### 5. 마켓 스캐너 도구 페이지 - `src/pages/marketing/school/tools/MarketScannerTool.tsx`
- `ITEM_TYPE_OPTIONS` 배열 추가 (8개: service, physical, digital, food, fashion, beauty, education, other)
- `itemType` state 추가 (default: 'other')
- 키워드 입력란 아래에 **아이템 형태** 드롭다운 추가
- `handleAnalyze()`에서 `generateMarketAnalysis(keyword, targetAge, targetGender, itemType)` 호출
- 결과 저장 시 `input.itemType` 포함
- `loadPreviousResult()`에서 `itemType` 복원
- 결과 화면에 **📋 분석 레포트** 섹션 추가 (`analysisReport` 있을 때만 표시)
  - `whitespace-pre-line` + 복사 버튼

### 6. 엣지 메이커 도구 페이지 - `src/pages/marketing/school/tools/EdgeMakerTool.tsx`
- `CompetitorInfo` 타입 import 추가
- `competitors` state 추가 (MarketScanner 결과에서 로드)
- 입력 화면에 **🏢 경쟁사 요약** 카드 추가 (competitors가 있을 때만)
  - 경쟁사 이름 + 설명 표시, 읽기 전용
- `handleGenerate()`에서 `generateBrandingStrategy(painPoints, strengths, competitors)` 호출
- 결과 저장 시 `input.competitors` 포함
- 이전 결과 로드 시 `competitors` 복원
- 결과 화면에 **📋 브랜딩 추천 레포트** 섹션 추가 (`brandingReport` 있을 때만)
  - `whitespace-pre-line` + 복사 버튼

---

## 미완료 작업 (❌)

### 7. i18n 번역 전체 업데이트 - `public/locales/ko/common.json`

**가장 큰 작업. 아래 내용 모두 추가/수정 필요:**

#### 7-1. 도구 이름 변경
```
school.periods.aptitudeTest.name → "적성 검사기"
school.periods.marketScanner.name → "시장 탐색기"
school.periods.edgeMaker.name → "브랜드 만들기"
```

#### 7-2. 적성검사 질문 45개 (3세트 × 15문항)
- 키 패턴: `school.aptitude.questions.set1.Q1.situation`, `.optionA`, `.optionB`
- ~ `school.aptitude.questions.set3.Q15.situation`, `.optionA`, `.optionB`
- **중요**: 일상 상황 기반 인간 성향 질문 (마케팅 실무 X), ~해요 대화체
- 기존 `school.aptitude.questions.Q1~Q15` → `set1.Q1~Q15`로 이동

#### 7-3. 페르소나 이름/타이틀/설명
```
school.aptitude.personas.CEO.name → "큰그림 그리는 사람"
school.aptitude.personas.CEO.title → "브랜드 마케팅"
school.aptitude.personas.CEO.desc → (10줄, ~해요 체, 마케팅 영역 언급)

school.aptitude.personas.PM.name → "이야기 만드는 사람"
school.aptitude.personas.PM.title → "콘텐츠 마케팅"
school.aptitude.personas.PM.desc → (10줄)

school.aptitude.personas.CPO.name → "숫자로 말하는 사람"
school.aptitude.personas.CPO.title → "퍼포먼스 마케팅"
school.aptitude.personas.CPO.desc → (10줄)

school.aptitude.personas.CMO.name → "사람 모으는 사람"
school.aptitude.personas.CMO.title → "CRM 마케팅"
school.aptitude.personas.CMO.desc → (10줄)

school.aptitude.personas.CSL.name → "물건 파는 사람"
school.aptitude.personas.CSL.title → "라이브커머스"
school.aptitude.personas.CSL.desc → (10줄)
```

#### 7-4. 페르소나 강점 (각 3개)
```
school.aptitude.personas.{ID}.strength1~strength3
```

#### 7-5. 뱃지 버튼 이름
```
school.aptitude.result.saveBadgeButton → "학생증에 적성뱃지 등록하기"
```

#### 7-6. 마켓 스캐너 새 키
```
school.marketCompass.scanner.itemType → "아이템 형태"
school.marketCompass.scanner.itemTypes.service → "서비스"
school.marketCompass.scanner.itemTypes.physical → "실물 제품"
school.marketCompass.scanner.itemTypes.digital → "디지털 제품"
school.marketCompass.scanner.itemTypes.food → "식품/음료"
school.marketCompass.scanner.itemTypes.fashion → "의류/패션"
school.marketCompass.scanner.itemTypes.beauty → "뷰티/화장품"
school.marketCompass.scanner.itemTypes.education → "교육/강의"
school.marketCompass.scanner.itemTypes.other → "기타"
school.marketCompass.scanner.result.analysisReportTitle → "📋 분석 레포트"
```

#### 7-7. 엣지 메이커 새 키
```
school.marketCompass.edgeMaker.competitorSummaryTitle → "경쟁사 요약"
school.marketCompass.edgeMaker.competitorSummaryFrom → "시장 탐색기 분석 결과에서 가져왔어요"
school.marketCompass.edgeMaker.result.brandingReportTitle → "📋 브랜딩 추천 레포트"
```

### 8. 빌드 검증
```bash
cd kiosk && npm run build
```
- TypeScript 타입 에러 확인
- i18n 키 누락 확인
- 빌드 성공 후 배포 가능

---

## 수정 파일 목록

| # | 파일 | 상태 | 변경 내용 |
|---|------|------|-----------|
| 1 | `src/types/school.ts` | ✅ 완료 | 타입 확장 (optional 필드 추가) |
| 2 | `src/data/aptitudeQuestions.ts` | ✅ 완료 | 3세트 질문 구조 + 헬퍼 함수 |
| 3 | `src/services/gemini/marketCompassService.ts` | ✅ 완료 | AI 프롬프트 + mock 데이터 |
| 4 | `src/pages/.../AptitudeTestTool.tsx` | ✅ 완료 | 세트 선택 + 결과 표시 |
| 5 | `src/pages/.../MarketScannerTool.tsx` | ✅ 완료 | 아이템 형태 + 분석 레포트 |
| 6 | `src/pages/.../EdgeMakerTool.tsx` | ✅ 완료 | 경쟁사 요약 + 브랜딩 레포트 |
| 7 | `public/locales/ko/common.json` | ❌ 미완료 | i18n 번역 전체 (45문항 등) |

---

## 설계 원칙
- 모든 새 필드는 `?` (optional) → 기존 localStorage 데이터 깨지지 않음
- 질문은 일상 성향 테스트 (마케팅 실무 지식 X)
- ~해요 대화체 한국어 (외국인 대상)
- 10줄 페르소나 설명, 20줄 분석/브랜딩 레포트
- mock 데이터로 API 없이도 동작

---

## 참고: 계획 파일
상세 구현 계획은 별도 저장됨:
`C:\Users\wodnj\.claude\plans\steady-herding-shamir.md`
